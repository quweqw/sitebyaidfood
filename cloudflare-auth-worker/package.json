{
  "name": "aifood-auth-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker auth API for AI Food/Cremenality.",
  "scripts": {
    "check": "node --check src/index.js && node --check src/cloud-core.js && node scripts/smoke-check.mjs && node scripts/crm-smoke-check.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create aifood-auth",
    "d1:migrate:local": "wrangler d1 migrations apply aifood-auth --local",
    "d1:migrate:prod": "wrangler d1 migrations apply aifood-auth --remote"
  },
  "devDependencies": {
    "wrangler": "^4.20.5"
  }
}
